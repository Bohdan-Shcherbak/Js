// Функция возвращает чётное или нечётное

// const even_or_odd = n => (n % 2) ? 'Odd' : 'Even';

const even_or_odd = (number) => { if(number % 2 == 0){return 'Even'} else{return 'Odd'}}

console.log(even_or_odd(0));
